<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/match_match"
    tools:context=".BuscarPedirUbicacionActivity">

    <TextView
        android:id="@+id/pedir_ubic_titulo"
        style="@style/estilo_comun_textos"
        android:textSize = "40sp"
        android:text="Buscar por ubicación" />

    <TextView
        android:id="@+id/pedir_ubic_contenedor_sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:padding="5dp"
        android:text="Seleccione ubicación:"
        android:textColor="@color/colorPrimaryDark"
        android:textSize="20dp"
        android:textStyle="bold"
        android:layout_centerInParent="true"
        android:layout_below="@+id/pedir_ubic_titulo"/>

    <Spinner
        android:id="@+id/pedir_ubic_spinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/pedir_ubic_contenedor_sp"/>

    <Button
        android:id="@+id/pedir_ubic_boton_buscar"
        style="@style/estilo_boton"
        android:text="Buscar"
        android:onClick="buscar"
        android:layout_below="@+id/pedir_ubic_spinner"/>
</RelativeLayout>